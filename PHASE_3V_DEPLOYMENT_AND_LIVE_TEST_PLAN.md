# Phase 3V deployment and live test plan

DO NOT DEPLOY until separately authorized and the exact candidate commit is verified present on GitHub feature/letter-memory-titles. Resolve local candidate with git rev-parse feature/letter-memory-titles; compare remote SHA. Verify baseline remote points exactly at aab4a5743260252a74030a07e340c2b056c1723a. No deployment should use uncommitted source.

Rerun npm run check:gift, npm run typecheck, npm run build, git diff --check. Verify server-only exact two-member allowlist and current audience without changing unrelated values. Push source to Sites managed repository only as required by its workflow; build/package/save/deploy the identical GitHub-pushed candidate. Record actual version, deployment ID, commit and environment revision. No migration.

Use independent actual A anonymous, U third nonmember, C bounniecrisis@gmail.com and Y changyue960915@gmail.com sessions. Never inject identity headers or paste credentials into reports. Prove trusted runtime ID/email, especially external Y without publisher rights. Missing account evidence blocks release.

For all four profiles check root HTML/RSC/hydration and network; private APIs photos/entries/wishes/assets/journey/backup; known existing media/photo/video/audio IDs privately retained. GET/HEAD/valid and invalid Range: A/U denied before bytes or size, C/Y allowed with private/no-store/nosniff. Confirm no cross-user cache replay after C/Y warms root/API/media. Test logout, focus, protected denial, back/forward/BFCache and multiple tabs; stale data must disappear.

Future Letters: membership plus server-time lock for draft/sealed/unlocked, known locked attachment stays locked, browser clock irrelevant. Backup confirmation required; no restore. Do not export real full archive unless separately justified and kept private.

Use only timestamped synthetic PHASE3V_TEST records for authorized writes. C creates title override for an unused chapter; Y reads and updates it. A/U must retain built-in title in HTML and receive no private entry or title. Check header, nav accessibility label, photo pairing, settings and paired-photo PDF. Page 6 reset must restore 一生 / Our Life to Come. Test stale revision from two editing states, 375px/enlarged text, close-button focus and literal script text. Never overwrite an existing personal override for testing; if all pages used, obtain a safe testing plan before writes.

Poetry: synthetic CRUD, separate comment preservation, stale conflict, snapshot copy unaffected by collection edits/deletion, custom text/reset/XSS and A/U denial. Test authorized application writes with synthetic Memory/Wish/nickname/plan only; clean up solely newly created synthetic records and overrides. Do not alter real unlock dates or personal media.

Release workflow:

1. Deploy exact GitHub-pushed Version 9 candidate.
2. Complete A/U/C/Y live verification and all critical privacy/media/lock/cache/title/poetry checks.
3. If RELEASE VERIFIED: synchronize GitHub main to the exact deployed source and create/push site-v9 only if no conflict.
4. Otherwise leave main unchanged and record blocker. Contain confirmed privacy leakage; never roll back to known-insecure pre-Phase-1 code.

Main may have divergent history: inspect ancestry first. Do not force-push or create a merge commit and call it the same source SHA. If advancing main to the exact candidate requires a non-fast-forward update, stop for a separately approved reconciliation strategy. No automatic history rewrite.
